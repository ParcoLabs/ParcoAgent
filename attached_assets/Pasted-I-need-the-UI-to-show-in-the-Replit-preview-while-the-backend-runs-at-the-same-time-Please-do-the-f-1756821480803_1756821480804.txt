I need the UI to show in the Replit preview while the backend runs at the same time. Please do the following EXACT file edits and only these:

1) Root: package.json
- Ensure it is a workspaces root:
  "workspaces": ["apps/*", "packages/*"]
- Replace the "scripts" block with:
  "scripts": {
    "dev": "npm-run-all -p dev:web dev:api",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace apps/api run dev",
    "build": "npm-run-all build:shared build:api build:web",
    "build:web": "npm --workspace apps/web run build",
    "build:api": "npm --workspace apps/api run build",
    "build:shared": "npm --workspace packages/shared run build"
  }
(Do not change any other fields.)

2) Root: .replit
- Set the run command to:
  run = "npm run dev"

3) Frontend: create or update apps/web/package.json with:
{
  "name": "parco-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

4) Frontend: create or update apps/web/tsconfig.json with:
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@shared/*": ["../../packages/shared/src/*"]
    }
  },
  "include": ["src", "vite.config.*", "index.html"]
}

5) Frontend: create or update apps/web/vite.config.ts to run Vite on 3000 and proxy /api to 4000:
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    host: "0.0.0.0",
    proxy: { "/api": "http://localhost:4000" }
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
      "@shared": path.resolve(__dirname, "../../packages/shared/src")
    }
  }
});

6) Frontend: ensure apps/web/index.html exists with:
<!doctype html>
<html>
  <head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width, initial-scale=1.0"/><title>Parco PM</title></head>
  <body><div id="root"></div><script type="module" src="/src/main.tsx"></script></body>
</html>

7) Backend: apps/api/package.json
- Remove "type": "module" if it exists.
- Ensure scripts include:
  "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
  "build": "tsc -p tsconfig.json"

8) Backend: apps/api/tsconfig.json
- Ensure CommonJS:
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "baseUrl": ".",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "esModuleInterop": true,
    "paths": { "@shared/*": ["../../packages/shared/src/*"] }
  },
  "include": ["src"]
}

9) Backend: apps/api/src/index.ts
- Ensure it listens once on port 4000:
import express from "express";
import cors from "cors";
const app = express();
app.use(cors());
app.use(express.json());
app.get("/api/health", (_req, res) => res.json({ ok: true }));
const PORT = 4000;
app.listen(PORT, () => console.log(`API listening on http://localhost:${PORT}`));

10) Disable any custom server that still points to client/ (old template).
- If a /server or similar folder exists that starts Vite or reads client/index.html, rename it to _server_old so it does not run.

11) Make sure all frontend fetch calls use relative paths like fetch("/api/..."), not hard-coded localhost ports.

Stop after applying these edits and tell me you're done.
